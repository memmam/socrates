<!--
Stub only: no content of its own. Its purpose is to give this directory
its own entry in Claude Code's nested-memory listing (the /memory
command and Claude Desktop's context tracker), lazily loaded the first
time Claude reads a file under ports/, so README.md and the per-port
contract/README files surface as tracked memory instead of only being
prose pointers in the root CLAUDE.md. See root CLAUDE.md's "Where the
detailed memory lives" section for what each imported file is for.
-->

@../README.md
@../pyl/CONTRACT.md
@../icaa/README.md
@../claudewave/README.md

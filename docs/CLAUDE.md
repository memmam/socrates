<!--
Stub only: no content of its own (see root CLAUDE.md's "Where the
project's memory lives" section for what each imported file holds).
Committed and tracked. Its purpose is purely to give this directory
its own entry in Claude Code's nested-memory listing (the /memory
command and Claude Desktop's context tracker), loaded the first
time Claude reads a file under docs/, so SPEC.md/ARCHITECTURE.md/
RELEASING-macOS.md surface as tracked memory instead of only being
prose pointers in the root CLAUDE.md.
-->

@SPEC.md
@ARCHITECTURE.md
@RELEASING-macOS.md

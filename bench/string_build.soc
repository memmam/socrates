// Bench: string accumulation via strings.Builder — 1M push_char calls
// into one builder, then a single build() join.
import std.strings;
let b = strings.builder();
for i in 0..1000000 {
    b.push_char(97 + (i & 15));
}
let s = b.build();
println("{s.len()} {s.code_at(999999).unwrap()}");

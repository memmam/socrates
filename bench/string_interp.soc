// Bench: string interpolation + short-string churn — 200k two-hole
// interpolations, each result immediately measured and dropped.
let mut n = 0;
for i in 0..200000 {
    let s = "row {i}: value = {i * 7 % 93}";
    n += s.len();
}
println(n);

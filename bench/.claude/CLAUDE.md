<!--
Stub only: no content of its own. Its purpose is to give this directory
its own entry in Claude Code's nested-memory listing (the /memory
command and Claude Desktop's context tracker), lazily loaded the first
time Claude reads a file under bench/, so RESULTS.md surfaces as
tracked memory instead of only being a prose pointer in the root
CLAUDE.md. See root CLAUDE.md's "Where the detailed memory lives"
section for what RESULTS.md holds.
-->

@../RESULTS.md

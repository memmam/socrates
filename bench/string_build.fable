// Bench: string accumulation via strings.Builder (1M chars).
import std.strings;
let b = strings.builder();
let mut i = 0;
while i < 1000000 {
    b.push_char(97 + (i & 15));
    i += 1;
}
let s = b.build();
println("{s.len()} {s.code_at(999999).unwrap()}");

<!--
Stub only: no content of its own (see root PROJECT.md's "Where this
project's memory lives" section for what each imported file holds).
Committed and tracked. Its purpose is purely to give this directory its own
entry in Claude Code's nested-memory listing (the /memory command and
Claude Desktop's context tracker), loaded the first time Claude
reads a file under bench/, so RESULTS.md surfaces as tracked memory
instead of only being a prose pointer in the root PROJECT.md.
-->

@RESULTS.md

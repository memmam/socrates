// Unbounded recursion panics with "stack overflow" (exit code 70); output
// produced before the panic must still appear.
//? panic: stack overflow
fn boom(n: Int) -> Int { boom(n + 1) }
println("starting");       //? expect: starting
println(boom(0));

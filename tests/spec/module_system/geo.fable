// A helper module for the module-system tests. Run standalone (as the spec
// harness does for every .fable file) it defines everything silently.
struct Point {
    x: Float,
    y: Float,
}

impl Point {
    fn dist(self, other: Point) -> Float {
        let dx = self.x - other.x;
        let dy = self.y - other.y;
        (dx * dx + dy * dy).sqrt()
    }
}

enum Shape {
    Circle(Float),
    Rect(Float, Float),
    Empty,
}

fn area(s: Shape) -> Float {
    match s {
        Shape.Circle(r) -> math.pi * r * r,
        Shape.Rect(w, h) -> w * h,
        Shape.Empty -> 0.0,
    }
}

fn origin() -> Point {
    Point { x: 0.0, y: 0.0 }
}

fn parse_coord(s: String) -> Option[Float] {
    // Reuses this module's own names freely.
    s.parse_float()
}

let unit_square = Shape.Rect(1.0, 1.0);
let mut bumps = 0;

fn bump() -> Int {
    bumps += 1;
    bumps
}

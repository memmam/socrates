// SPEC 1.4: characters outside the language get a targeted lexer error.
// (`&` and `^` became bitwise operators in v0.7, so `@` carries this test.)
//? error: E0100
//? error: unexpected character `@`
let y = @;

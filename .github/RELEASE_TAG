v0.8.0
# First line is the release tag (read via `head -1`). Any push to main that
# changes this file re-fires the Release workflow, re-cutting the draft against
# the current commit — used here to re-point the draft target at current main.
# v0.8.0: the v0.7 demo round's feature queue (if let/while let, bitwise
# compound assignment, 64-bit hex literals, Bytes accessors, wrapping
# arithmetic, std.lazy, --bless) PLUS the native graphics-and-compute
# programme: window/gfx across OpenGL, Metal, and Vulkan (glcube
# byte-identical on all three), five native compute backends, std.glm,
# wgpu deleted — every build zero-dependency.
# Folded in before publication: the rename to Socrates (`.soc` extension,
# SOCRATES_* env vars) and the minification + consistency passes (the
# four-arch Bench A/B gate, compact dispatch with per-target binding,
# math namespace minified, docs/ports/bench reconciled to CI).
